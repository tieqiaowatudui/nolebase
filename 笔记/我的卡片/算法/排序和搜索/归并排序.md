归并排序是将一个序列不断的一分为二,直到还是一个元素就变成了有序.然后将两个有序的部分数组进行合并,合并到最后得到的就是一个有序的序列.

```python
MergeSort(list):
	MergeSort(list[0,len(list)//2])
	MergeSort(list[len(list)//2+1:])
	merge(list,0,len(list)//2,len(list)-1)
```

根据上面的算法实现,可以看出来归并排序是典型的分治算法.不断的分为两部分,然后进行合并.