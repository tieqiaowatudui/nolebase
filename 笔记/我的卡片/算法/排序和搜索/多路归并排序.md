在工程上,可能待排序列无法完全读到内存中,这个时候就需要外存排序.
# 算法实现
将待排序列分块,大小为能够在内存中完成排序,然后写入磁盘上.假设这个排好序的块的个数是k.
然后依次将每一个块中读取一个元素到内存中,这样就一共有k个元素,将这个k个元素组成一个堆.
然后从堆中取出最小的元素,且记录住这个最小元素来源的块.然后将最小的元素写入到外存的排序结果中,同时从对应的块中取出最小元素(其实是第二小),然后重复上面的操作

# 正确性
因为k个块中,每个块中的元素都是升序的,每次在内存中的堆中的k个元素都是k给块中最小的元素,那么每次从堆中取出的最小元素就是所有块中的最小yua