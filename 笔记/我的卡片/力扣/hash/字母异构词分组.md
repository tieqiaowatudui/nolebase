---
tags: [LeetCode, 算法, Python3]
aliases: []
---



## 1️⃣ 基本信息
- **题目路径**：[字母异构词分组](https://leetcode.cn/problems/group-anagrams/)
- **题目难度**：中等
- **标签/分类**：

---

## 2️⃣ 题目描述
- **输入**：
- **输出**：
- **样例**：

> 给定一个字符串数组,将这些字符串中有相同结构的词放在一个list中.最终返回一个字符串数组的数组.其中同构是指一个字符串有相同的字母组成,且字母的个数也相同

---

## 3️⃣ 解题思路
### 思路1（用字符串排序法生成字符串的结构）
- 算法原理：首先将字符串按照组成的字母升序排序,然后重新组成一个字符串.这样同构的字符串就是完全相同的.
- 时间复杂度：
- 空间复杂度：

### 思路2（使用位计算的方式实现.）
- 算法原理：使用长度为26的数组,将字符串中的每一位依次计数.然后以一个tuple类来封装
- 时间复杂度：
- 空间复杂度：

---

## 4️⃣ Python3 技巧点
- **语法技巧**：
  - 获取某一个字符的unicode.`ord(s)`.
  - 根据一个数值,获取到对应的字符.`chr(97)` = "a"
  - `defaultdict`能够帮助构建一种特殊的dict.当key不存dict中的时候,会帮助返回一个默认的空对象.
  - `[0] * 3`生成一个`[0,0,0]`的数组
  - 对于生成二维数组的实现.`[[0] * 3] * 3 会生成三个引用的二维数组,其实是一个数组的引用出现了三次`真实的写法是 `[[0] * 3  for _ in range(3)]`这样就生成了 三个不同的以为数组
- **数据结构**：
  - `tuple`:一个不可变的序列元素.不可变所以能够生成hash.`list`由于能变,所以hashzhi'jie
  - 
- **常用方法**：
  - 切片 `nums[i:j]`
  - `lambda` + `sorted`/`heapq`
- **注意事项**：
  - 浅拷贝和深拷贝
  - 默认可变参数问题

---

## 5️⃣ 算法技巧点
- **算法类别**：回溯 / 动态规划 / 贪心 / 二分 / DFS / BFS / 双指针
- **常用模板**：
  - 回溯模板：
    ```python
    def backtrack(path, choices):
        if 满足条件:
            res.append(path)
            return
        for choice in choices:
            backtrack(path + [choice], choices - ...)
    ```
  - 动态规划模板：
    ```python
    dp = [0] * n
    for i in range(n):
        dp[i] = min/max/... dp[j] + ...
    ```

---

## 6️⃣ 代码实现
```python
class Solution:
    def function_name(self, params):
        # TODO: 实现代码
        pass
