---
tags: [LeetCode, 算法, Python3]
aliases: []
---



## 1️⃣ 基本信息
- **题目路径**：[最长子序列和](https://leetcode.cn/problems/maximum-subarray/)
- **题目难度**： 中等 
- **标签/分类**：贪心

---

## 2️⃣ 题目描述
- **输入**：一个数组
- **输出**：其中最长子序列的和
- **样例**：

> 一个数组,求出其中最长连续子序列的和.

---

## 3️⃣ 解题思路
### 思路1（动态规划）
- 算法原理：在统计到第i个数的时候,是否需要将前面的第 i-1个数添加到最长子序列和的判定条件是前面的
- 时间复杂度：
- 空间复杂度：

### 思路2（优化/进阶）
- 算法原理：
- 时间复杂度：
- 空间复杂度：

---

## 4️⃣ Python3 技巧点
- **语法技巧**：
  - 例：列表生成式 `[x*x for x in nums]`
  - 例：字典操作 `dict.get(key, default)`
  - 内置函数：`zip`, `enumerate`, `sorted`, `heapq`
- **数据结构**：
  - `deque` 双端队列
  - `set` 与 `dict` 的差异
- **常用方法**：
  - 切片 `nums[i:j]`
  - `lambda` + `sorted`/`heapq`
- **注意事项**：
  - 浅拷贝和深拷贝
  - 默认可变参数问题

---

## 5️⃣ 算法技巧点
- **算法类别**：回溯 / 动态规划 / 贪心 / 二分 / DFS / BFS / 双指针
- **常用模板**：
  - 回溯模板：
    ```python
    def backtrack(path, choices):
        if 满足条件:
            res.append(path)
            return
        for choice in choices:
            backtrack(path + [choice], choices - ...)
    ```
  - 动态规划模板：
    ```python
    dp = [0] * n
    for i in range(n):
        dp[i] = min/max/... dp[j] + ...
    ```

---

## 6️⃣ 代码实现
```python
class Solution:
    def function_name(self, params):
        # TODO: 实现代码
        pass
